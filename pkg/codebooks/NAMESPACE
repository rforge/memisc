useDynLib(codebooks)
import(stats,methods,mutils)
importFrom(utils,toLatex,head,tail,capture.output)
importFrom(mtable,fapply,Table,percent)
export(
      contr,
      contr.treatment,
      contr.sum,
      recode,
      Descriptives,
      as.item,
      labels,
      "labels<-",
      value.filter,
      valid.values,
      missing.values,
      valid.range,
      "valid.values<-",
      "valid.range<-",
      "missing.values<-",
      measurement,
      "measurement<-",
      include.missings,
      annotation,
      "annotation<-",
      description,
      "description<-",
      wording,
      "wording<-",
      is.missing,
      is.valid,
      nvalid,
      is.nominal,
      is.ordinal,
      is.interval,
      is.ratio,
      contrasts,
      "contrasts<-",
      data.set,
      as.data.set,
      is.data.set,
      codebook,
      spss.fixed.file,
      spss.portable.file,
      spss.system.file,
      Stata.file,
      sample,
      unique,
      query,
      within.data.frame
      )

exportClasses(
  atomic,
  value.labels,
  value.filter,
  valid.values,
  valid.range,
  missing.values,
  annotation,
  item,
  double,
  integer.item,
  double.item,
  character.item,
  numeric.item,
  datetime.item,
  item.vector,
  named.list,
  item.list,
  data.set,
  spss.fixed.importer,
  spss.portable.importer,
  spss.system.importer,
  Stata.importer,
  importer,
  codebook
  )
exportMethods(
    as.item,
    labels,
    "labels<-",
    value.filter,
    valid.values,
    missing.values,
    valid.range,
    "valid.values<-",
    "valid.range<-",
    "missing.values<-",
    measurement,
    "measurement<-",
    annotation,
    "annotation<-",
    Arith,
    Compare,
    Math,
    Math2,
    Summary,
    weighted.mean,
    format,
    print,
    show,
    unique,
    "[",
    "[<-",
    row.names,
    dimnames,
    as.vector,
    as.character,
    as.numeric,
    as.integer,
    as.factor,
    as.ordered,
    contrasts,
    "contrasts<-",
    as.matrix,
    summary,
    within,
    merge,
    "%in%",
    "%nin%",
    recode,
    dim,
    subset,
    initialize,
    coerce,
    Descriptives,
    Table,
    codebook,
    path,
    query#,
    #View
    )
S3method(as.data.frame,character.item)
S3method(as.data.frame,double.item)
S3method(as.data.frame,integer.item)
S3method("[[<-",data.set)
S3method(str,data.set)
S3method(sort,data.set)
S3method(str,integer.item)
S3method(str,double.item)
S3method(str,character.item)
S3method(str,datetime.item)
S3method(as.POSIXct,datetime.item)
S3method(as.data.frame,datetime.item)
S3method(as.list,data.set)
S3method(as.data.frame,data.set)
S3method(cbind,data.set)
S3method(rbind,data.set)
S3method(fapply,data.set)
S3method(edit,data.set)
S3method(print,descriptions)
S3method(xtfrm,integer.item)
S3method(xtfrm,double.item)
S3method(xtfrm,character.item)
S3method(xtfrm,datetime.item)
S3method(unique,integer.item)
S3method(unique,double.item)
S3method(unique,character.item)
S3method(unique,datetime.item)
S3method(Table2,item.vector)
S3method(percent2,item.vector)
